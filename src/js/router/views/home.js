import { createFeaturedSection } from './components/featured.js';

/**
 * Initializes the home page by creating the featured listings section
 * @function
 * @description Creates and displays the featured listings section on the home page
 */
createFeaturedSection();
